name: تست آپلود - با action جدید

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-upload:
    runs-on: ubuntu-latest

    steps:
      - name: دریافت فقط پوشه مورد نظر
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            downloads/batch_20260428_202253_7
          sparse-checkout-cone-mode: false

      - name: بررسی پوشه
        run: |
          echo "========== محتویات پوشه =========="
          ls -lhR downloads/batch_20260428_202253_7/
          TOTAL_SIZE=$(du -sh downloads/batch_20260428_202253_7/ | cut -f1)
          echo "📊 حجم: $TOTAL_SIZE"

      - name: ساخت ZIP
        run: |
          TIMESTAMP=$(date +'%Y%m%d_%H%M%S')
          ZIP_NAME="test_new_action_${TIMESTAMP}.zip"
          
          echo "📦 در حال فشرده‌سازی..."
          zip -r "${ZIP_NAME}" downloads/batch_20260428_202253_7/ > /dev/null 2>&1
          
          ZIP_SIZE=$(du -h "$ZIP_NAME" | cut -f1)
          echo "✅ فایل ZIP: $ZIP_NAME ($ZIP_SIZE)"
          echo "ZIP_NAME=$ZIP_NAME" >> $GITHUB_ENV

      - name: آپلود به Google Drive (action جدید)
        uses: adityak74/google-drive-upload-action@main
        with:
          credentials: ${{ secrets.GDRIVE_CREDENTIALS }}
          filename: ${{ env.ZIP_NAME }}
          folderId: ${{ secrets.GDRIVE_FOLDER_ID }}
          name: ${{ env.ZIP_NAME }}
          overwrite: true

      - name: گزارش نهایی
        run: |
          echo ""
          echo "═════════════════════════════════════════════"
          echo "🎉 تست آپلود انجام شد!"
          echo "📁 فایل: ${{ env.ZIP_NAME }}"
          echo "☁️  مقصد: Google Drive"
          echo "═════════════════════════════════════════════"
